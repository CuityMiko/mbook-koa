import { FriendHelp, Book, User } from '../models'

export default function(router) {
  router.post('/api/friend_help', async (ctx, next) => {
    // todo
  })

  router.get('/api/friend_help', async (ctx, next) => {
    // todo
  })

  router.put('/api/friend_help/:id', async (ctx, next) => {
    // todo
  })

  router.delete('/api/friend_help/:id', async (ctx, next) => {
    // todo
  })
}
